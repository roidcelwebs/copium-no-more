# Cloud archive (disabled)

The application currently runs as a fully local browser prototype. This folder preserves the last
Supabase/Lovable Cloud migrations and Edge Functions for reference only. Nothing under this folder
is imported by the application or deployed during the local-only phase.

Before a public launch, design a new authenticated Cloud migration instead of assuming this archive
is production-ready.
